// Next.js resolves the bare "server-only" specifier at bundle time; it is not
// an installed package. Tests run these modules in plain Node, so tests/
// tsconfig.json maps the specifier here. No-op on purpose.
export {};
